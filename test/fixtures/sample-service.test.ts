// @test-approach: tdd
// test/fixtures/sample-service.test.ts
// Fixture: demonstrates TDD annotation for unit tests routed to subagents

describe('FEAT-SAMPLE: Sample Service', () => {
  describe('AC-FEAT-SAMPLE-001: Basic validation', () => {
    it('should validate input format', () => {
      // Given: an invalid input string
      // When: validation is called
      // Then: it returns false with error message
      throw new Error('Test not implemented');
    });
  });
});
